# Retrieval microservice connection (Fly.io iad)
RETRIEVAL_SERVICE_URL=https://detailagent-retrieval.fly.dev

# Shared-secret for Bearer auth; must match RETRIEVAL_SHARED_SECRET
# configured on the microservice (fly secrets list). Length >= 16.
# Generated with: openssl rand -base64 24
RETRIEVAL_SHARED_SECRET=
