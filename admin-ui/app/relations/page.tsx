import { PagePlaceholder } from "@/components/shell/placeholder";

export default function RelationsPage() {
  return (
    <PagePlaceholder title="Ürün İlişkileri" phase="Phase 4.9.6">
      1 301 relation · 5 granular tip (use_with, use_before, use_after, accessories, alternatives).
      react-flow graph görünümü + SKU-bazlı editor.
    </PagePlaceholder>
  );
}
