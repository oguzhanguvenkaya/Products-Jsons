import { PagePlaceholder } from "@/components/shell/placeholder";

export default function CommitPage() {
  return (
    <PagePlaceholder title="Commit Workflow" phase="Phase 4.9.8">
      SQL preview · tek transaction · audit log · rollback. Onaydan sonra
      retrieval-service <code className="font-mono text-xs">/admin/staging/commit</code>{" "}
      endpoint'ine akar.
    </PagePlaceholder>
  );
}
