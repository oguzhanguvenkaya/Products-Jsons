import { PagePlaceholder } from "@/components/shell/placeholder";

export default function HeatmapPage() {
  return (
    <PagePlaceholder title="Coverage Heatmap" phase="Phase 4.9.2">
      26×40 matrix (template_group × specs key) — renk yoğunluğu null oranını gösterecek.
      Tıklanabilir hücreler ilgili ürün filtrelerine dallanır.
    </PagePlaceholder>
  );
}
