import { PagePlaceholder } from "@/components/shell/placeholder";

export default function StagingPage() {
  return (
    <PagePlaceholder title="Staging Drawer" phase="Phase 4.9.8">
      Bekleyen değişiklikler listesi · alan bazlı diff · atomic revert. Store:
      Zustand + localStorage (persistent).
    </PagePlaceholder>
  );
}
